# Yuktasriya Vuppula — Portfolio (Static Site)

This folder contains a simple static portfolio website (HTML/CSS/JS) you can host for free.

## What's inside
- `index.html` — main landing page
- `projects/` — 4 case study pages (edit these with your real screenshots + metrics)
- `assets/` — your PDFs (resume + LinkedIn export)
- `css/styles.css` — styling
- `js/main.js` — small helper script

## How to run locally
Open `index.html` in your browser.

## Publish on GitHub Pages (recommended)
1. Create a GitHub repo (example: `yuktasriya-portfolio`)
2. Upload all files (keep the folder structure)
3. In GitHub: **Settings → Pages → Deploy from branch**
4. Select branch `main` and folder `/root`

## What to update first
1. Add 1–2 screenshots per project.
2. Add 1–2 measurable results per project (hours saved, refresh time improvement, adoption count, etc.).
3. Replace placeholder text with your real story.

Generated on 2026-02-04.
# portfolio
